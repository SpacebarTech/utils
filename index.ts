/**
 * Utils
 *
 * First, import everything into the `utils` namespace and
 * export as default so that the user can do the following:
 *
 *   import utils from "@jetstech/utils"
 *
 * Then export the remaining members so that the user can
 * also do:
 *
 *   import { clone } from "@jetstech/utils";
 */

import * as utils from './src/index';

export default utils;
export * from './src/index';
