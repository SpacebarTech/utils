import { assocByKey } from '../..';

/**
 * objToArray
 * @deprecated
 *
 * Transforms an object with nested objects into an array
 * of objects, where the specified top-level key becomes a
 * property of that object.
 */

export const objToArray = assocByKey;
